describe("Select", () => {
  describe("Basic", () => {
    it("should display select itself", () => {
      expect(1 + 2).toBe(3);
    });

    it("should display select and passed label", () => {
      expect(1 + 2).toBe(3);
    });

    it("should open dropdown on click", () => {
      expect(1 + 2).toBe(3);
    });

    it("should close dropdown click outside", () => {
      expect(1 + 2).toBe(3);
    });

    it("should not open on click if disabled", () => {
      expect(1 + 2).toBe(3);
    });

    it("it should use renderOption function if passed", () => {
      expect(1 + 2).toBe(3);
    });
  });

  describe("Dropdown", () => {
    it("should display all options", () => {
      expect(1 + 2).toBe(3);
    });

    it("should di", () => {
      expect(1 + 2).toBe(3);
    });
  });

  describe("Actions", () => {
    it("should call onChange, with the value", () => {
      expect(1 + 2).toBe(3);
    });

    it("should call onChange, with raw value with passing rawValue", () => {
      expect(1 + 2).toBe(3);
    });

    it("should call onBlur, on click outside", () => {
      expect(1 + 2).toBe(3);
    });
  });

  describe("Search", () => {
    it("should open dropdown on search", () => {
      expect(1 + 2).toBe(3);
    });

    it("should filter the data on the search", () => {
      expect(1 + 2).toBe(3);
    });

    it("should call passed onSearch func", () => {
      expect(1 + 2).toBe(3);
    });
  });
});
